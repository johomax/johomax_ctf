diff --git a/bot/baseline/shell_seat.nim b/bot/baseline/shell_seat.nim
index f896c0f..5bc2281 100644
--- a/bot/baseline/shell_seat.nim
+++ b/bot/baseline/shell_seat.nim
@@ -15,26 +15,34 @@ import
 const
   # The body applies all three native safety reflexes outside the guest ladder;
   # their reserved names are not call bindings and the validator rejects them.
-  TargetLawSurvivalEntry =
-    "{\"params\":{\"holdTrigger\":{\"aliveTeams\":8},\"prefer\":[\"weakened\",\"isolated\"]},\"play\":\"target_law\"}"
-  TargetLawFightEntry =
-    "{\"params\":{\"prefer\":[\"weakened\",\"isolated\"]},\"play\":\"target_law\"}"
+  TargetLawSurvivalPrefix =
+    "{\"params\":{\"holdTrigger\":{\"aliveTeams\":8},\"never\":[\""
+  TargetLawSurvivalSuffix =
+    "\"],\"prefer\":[\"weakened\",\"isolated\"]},\"play\":\"target_law\"}"
+  TargetLawFightPrefix = "{\"params\":{\"never\":[\""
+  TargetLawFightSuffix =
+    "\"],\"prefer\":[\"weakened\",\"isolated\"]},\"play\":\"target_law\"}"
   SupplyRunEntry =
-    "{\"params\":{\"contested\":\"avoid\",\"detourMax\":500,\"whenHpBelow\":3},\"play\":\"supply_run\",\"when\":[\"<\",[\"get\",\"self.hp_frac\"],0.67]}"
+    "{\"params\":{\"contested\":\"avoid\",\"detourMax\":500,\"whenHpBelow\":3},\"play\":\"supply_run\"}"
   BodyguardEntry =
-    "{\"params\":{\"interpose\":false,\"leash\":[80,220],\"peelHp\":2},\"play\":\"bodyguard\",\"when\":[\"<\",220,[\"get\",\"partner.dist\"]]}"
-  EdgeRideEntry =
-    "{\"params\":{\"coverBias\":1.0,\"enterLead\":120,\"margin\":220},\"play\":\"edge_ride\"}"
+    "{\"params\":{\"interpose\":false,\"leash\":[80,220],\"peelHp\":2},\"play\":\"bodyguard\"}"
   CrossfireEntry =
     "{\"params\":{\"minAngle\":32,\"spacing\":[120,320]},\"play\":\"crossfire\"}"
   JackalEntry =
     "{\"params\":{\"earshot\":500,\"exitAfter\":{\"kills\":1},\"joinWhen\":\"bothWeakened\"},\"play\":\"jackal\"}"
-  EdgeRideCall = "{\"plays\":[" & EdgeRideEntry & "]}"
+  BaseEdgeMargin = 220
+  PartnerMarginOffset = 80
+  EdgeCoverBias = 1.0
+  EdgeEnterLead = 120
   # Views normally arrive every six ticks, making 552 the last regular
   # decision point before the hard tick-560 standing-call deadline.
   StartupCallDeadlineTick = 552
   ZoneUrgencyTicks = 120
   VisibleFreshTicks = 12
+  PartnerFarDistance = 220.0
+  PartnerStackDistance = 40.0
+  PartnerLineHalfWidth = 16.0
+  DecisionDebounceViews = 2
 
 static:
   doAssert PlaybookEdgeRideBytes.len <= MaxModuleBytes
@@ -67,6 +75,9 @@ type
 
   StrategyPhase = enum
     phaseSurvival
+    phaseLowHp
+    phasePartnerFar
+    phaseZoneUrgent
     phaseCrossfire
     phaseJackal
 
@@ -83,6 +94,9 @@ type
     pendingCallJson: string
     pendingCallSmallest: bool
     activePhase: StrategyPhase
+    activeCallJson: string
+    candidatePhase: StrategyPhase
+    candidateViewCount: int
     standingAccepted: bool
     initialCallSent: bool
     retrySmallest: bool
@@ -168,6 +182,9 @@ proc stringValue(node: JsonNode; key: string): string =
 proc phaseName(phase: StrategyPhase): string =
   case phase
   of phaseSurvival: "survival"
+  of phaseLowHp: "low_hp"
+  of phasePartnerFar: "partner_far"
+  of phaseZoneUrgent: "zone_urgent"
   of phaseCrossfire: "crossfire"
   of phaseJackal: "jackal"
 
@@ -190,20 +207,60 @@ proc addCallEntry(entries: var seq[string]; seat: ShellSeat;
   if seat.moduleReady(name):
     entries.add(entry)
 
+proc targetLawEntry(seat: ShellSeat; survival: bool): string =
+  # The deployed call validator leaves its duo lookup unconfigured. Expand the
+  # conceptual own-duo veto to the two accepted direct seat references.
+  let
+    firstSeat = min(seat.slot, seat.partnerSeat)
+    secondSeat = max(seat.slot, seat.partnerSeat)
+    ownDuoRefs = "seat:" & $firstSeat & "\",\"seat:" & $secondSeat
+  if survival:
+    TargetLawSurvivalPrefix & ownDuoRefs & TargetLawSurvivalSuffix
+  else:
+    TargetLawFightPrefix & ownDuoRefs & TargetLawFightSuffix
+
+proc edgeMargin(seat: ShellSeat): int =
+  BaseEdgeMargin + (if seat.slot < 16: 0 else: PartnerMarginOffset)
+
+proc edgeRideEntry(seat: ShellSeat): string =
+  "{\"params\":{\"coverBias\":" & $EdgeCoverBias &
+    ",\"enterLead\":" & $EdgeEnterLead & ",\"margin\":" &
+    $seat.edgeMargin & "},\"play\":\"edge_ride\"}"
+
 proc phaseCall(seat: ShellSeat; phase: StrategyPhase): string =
   var entries: seq[string]
   case phase
   of phaseSurvival:
     entries.addCallEntry(seat, PlaybookTargetLawName,
-      TargetLawSurvivalEntry)
+      seat.targetLawEntry(true))
+    entries.addCallEntry(seat, PlaybookEdgeRideName, seat.edgeRideEntry)
+    entries.addCallEntry(seat, PlaybookBodyguardName, BodyguardEntry)
+    entries.addCallEntry(seat, PlaybookSupplyRunName, SupplyRunEntry)
+  of phaseLowHp:
+    entries.addCallEntry(seat, PlaybookTargetLawName,
+      seat.targetLawEntry(true))
     entries.addCallEntry(seat, PlaybookSupplyRunName, SupplyRunEntry)
+    entries.addCallEntry(seat, PlaybookEdgeRideName, seat.edgeRideEntry)
     entries.addCallEntry(seat, PlaybookBodyguardName, BodyguardEntry)
-    entries.addCallEntry(seat, PlaybookEdgeRideName, EdgeRideEntry)
+  of phasePartnerFar:
+    entries.addCallEntry(seat, PlaybookTargetLawName,
+      seat.targetLawEntry(true))
+    entries.addCallEntry(seat, PlaybookBodyguardName, BodyguardEntry)
+    entries.addCallEntry(seat, PlaybookEdgeRideName, seat.edgeRideEntry)
+    entries.addCallEntry(seat, PlaybookSupplyRunName, SupplyRunEntry)
+  of phaseZoneUrgent:
+    entries.addCallEntry(seat, PlaybookTargetLawName,
+      seat.targetLawEntry(true))
+    entries.addCallEntry(seat, PlaybookEdgeRideName, seat.edgeRideEntry)
+    entries.addCallEntry(seat, PlaybookBodyguardName, BodyguardEntry)
+    entries.addCallEntry(seat, PlaybookSupplyRunName, SupplyRunEntry)
   of phaseCrossfire:
-    entries.addCallEntry(seat, PlaybookTargetLawName, TargetLawFightEntry)
+    entries.addCallEntry(seat, PlaybookTargetLawName,
+      seat.targetLawEntry(false))
     entries.addCallEntry(seat, PlaybookCrossfireName, CrossfireEntry)
   of phaseJackal:
-    entries.addCallEntry(seat, PlaybookTargetLawName, TargetLawFightEntry)
+    entries.addCallEntry(seat, PlaybookTargetLawName,
+      seat.targetLawEntry(false))
     entries.addCallEntry(seat, PlaybookJackalName, JackalEntry)
   if entries.len > 0:
     result = "{\"plays\":[" & entries.join(",") & "]}"
@@ -234,7 +291,7 @@ proc startupCallDecision(seat: ShellSeat; tick: int): tuple[
 
 proc smallestRetryCall(seat: ShellSeat): string =
   if seat.retrySmallest and seat.moduleReady(PlaybookEdgeRideName):
-    EdgeRideCall
+    "{\"plays\":[" & seat.edgeRideEntry & "]}"
   else:
     ""
 
@@ -322,9 +379,11 @@ proc handleContext(seat: ShellSeat; ws: WebSocket; packet: ShellPacket) =
   if context.hasKey("self"):
     let own = context["self"]
     seat.partnerSeat = own.intValue("duo_partner", -1)
-    seat.selfTeam = own.stringValue("team").teamId
-    if seat.selfTeam < 0:
-      raise newException(ValueError, "invalid S2 context self team")
+    let teamName = own.stringValue("team")
+    seat.selfTeam = teamName.teamId
+    if seat.selfTeam < 0 or seat.partnerSeat notin 0 ..< 32 or
+        seat.partnerSeat == seat.slot:
+      raise newException(ValueError, "invalid S2 context self team or partner")
   seat.gunRange = context.floatValue("gun_range", seat.gunRange)
   log("context gen=" & $seat.generation & " upload_floor=" &
     $(seat.nextUploadId - 1) & " proposal_floor=" &
@@ -393,11 +452,15 @@ proc handleStatus(seat: ShellSeat; status: JsonNode) =
   of "call_accepted":
     let proposalId = status.uintValue("proposal_id")
     if proposalId == seat.pendingProposalId:
+      let acceptedCallJson = seat.pendingCallJson
       seat.pendingProposalId = 0
       seat.pendingCallJson.setLen(0)
       seat.pendingCallSmallest = false
       seat.retrySmallest = false
       seat.activePhase = seat.pendingPhase
+      seat.activeCallJson = acceptedCallJson
+      seat.candidatePhase = seat.activePhase
+      seat.candidateViewCount = 0
       seat.standingAccepted = true
       seat.phaseStartTick = status.intValue("tick")
       log("call accepted phase=" & seat.activePhase.phaseName & " id=" &
@@ -453,20 +516,45 @@ proc handleControl(seat: ShellSeat; ws: WebSocket; controlBytes: string;
     ws.send(statusAckBlob(seat.ackMark), BinaryMessage)
   seat.advanceStartup(ws, tick)
 
+proc zoneUrgent(seat: ShellSeat; view: StrategyView): bool =
+  if not view.zone.present:
+    return false
+  let
+    pos = view.self.pos
+    rect = view.zone.current
+    right = rect.x + rect.w
+    bottom = rect.y + rect.h
+  if pos.x < rect.x or pos.x >= right or pos.y < rect.y or pos.y >= bottom:
+    return true
+  let edgeDistance = min(min(pos.x - rect.x, right - pos.x),
+    min(pos.y - rect.y, bottom - pos.y))
+  view.zone.ticksToShrink <= ZoneUrgencyTicks or
+    edgeDistance < seat.edgeMargin
+
+proc partnerBlocksFightLine(view: StrategyView): bool =
+  let
+    mx = float(view.partner.pos.x - view.self.pos.x)
+    my = float(view.partner.pos.y - view.self.pos.y)
+  for enemy in view.visibleEnemies:
+    let
+      dx = float(enemy.pos.x - view.self.pos.x)
+      dy = float(enemy.pos.y - view.self.pos.y)
+      rangeSquared = dx * dx + dy * dy
+      alongNumerator = mx * dx + my * dy
+    if rangeSquared <= 0.0 or alongNumerator <= 0.0 or
+        alongNumerator >= rangeSquared:
+      continue
+    let cross = mx * dy - my * dx
+    if cross * cross <= PartnerLineHalfWidth * PartnerLineHalfWidth *
+        rangeSquared:
+      return true
+  false
+
 proc desiredPhase(seat: ShellSeat; view: StrategyView): StrategyPhase =
   if not view.self.alive:
     return phaseSurvival
 
-  let hpLow = view.self.hpFrac < 0.67
-  var zoneUrgent = false
-  if view.zone.present:
-    let
-      zone = view.zone
-      pos = view.self.pos
-      rect = zone.current
-    zoneUrgent = zone.ticksToShrink <= ZoneUrgencyTicks or
-      pos.x < rect.x or pos.x >= rect.x + rect.w or
-      pos.y < rect.y or pos.y >= rect.y + rect.h
+  let hpLow = view.self.hpFrac < 0.67 or view.self.hp <= 1
 
   var
     weakEnemies = 0
@@ -487,21 +575,48 @@ proc desiredPhase(seat: ShellSeat; view: StrategyView): StrategyPhase =
 
   let
     visibleEnemy = view.visibleEnemies.len > 0
-    partnerInShape = view.partner.positionKnown and view.partner.fresh and
-      view.partner.alive and view.partner.distance <= 600.0
-  if seat.activePhase != phaseSurvival and
-      (zoneUrgent or hpLow or seat.partnerDead or ownKill or not visibleEnemy):
+    partnerTracked = view.partner.positionKnown and view.partner.fresh and
+      view.partner.aliveKnown and view.partner.alive
+    partnerStacked = partnerTracked and
+      view.partner.distance < PartnerStackDistance
+    partnerFar = partnerTracked and view.partner.distance > PartnerFarDistance
+    partnerFightSafe = partnerTracked and
+      view.partner.distance > PartnerStackDistance and
+      view.partner.distance <= 600.0 and not view.partnerBlocksFightLine
+
+  if seat.partnerDead:
+    return phaseSurvival
+  if seat.zoneUrgent(view):
+    return phaseZoneUrgent
+  if hpLow:
+    return phaseLowHp
+  if partnerStacked:
     return phaseSurvival
-  if zoneUrgent or hpLow or seat.partnerDead:
+  if partnerFar:
+    return phasePartnerFar
+  if seat.activePhase != phaseSurvival and
+      (ownKill or not visibleEnemy or not partnerFightSafe):
     return phaseSurvival
-  if freshKill and weakEnemies >= 2 and
+  if freshKill and weakEnemies >= 2 and partnerFightSafe and
       seat.moduleReady(PlaybookJackalName):
     return phaseJackal
-  if visibleEnemy and weakEnemies >= 1 and partnerInShape and
+  if visibleEnemy and weakEnemies >= 1 and partnerFightSafe and
       seat.moduleReady(PlaybookCrossfireName):
     return phaseCrossfire
   phaseSurvival
 
+proc observePhase(seat: ShellSeat; wanted: StrategyPhase): bool =
+  if wanted == seat.activePhase:
+    seat.candidatePhase = wanted
+    seat.candidateViewCount = 0
+    return false
+  if seat.candidateViewCount == 0 or seat.candidatePhase != wanted:
+    seat.candidatePhase = wanted
+    seat.candidateViewCount = 1
+    return false
+  inc seat.candidateViewCount
+  seat.candidateViewCount >= DecisionDebounceViews
+
 proc handleView(seat: ShellSeat; ws: WebSocket; packet: ShellPacket) =
   seat.handleControl(ws, packet.control, packet.tick.int)
   if packet.view.len == 0 or not seat.standingAccepted or
@@ -516,16 +631,40 @@ proc handleView(seat: ShellSeat; ws: WebSocket; packet: ShellPacket) =
         " detail=" & decoded.detail)
     return
   let wanted = seat.desiredPhase(decoded.view)
-  if wanted != seat.activePhase:
-    let callJson = seat.phaseCall(wanted)
-    if callJson.len == 0:
-      log("phase skipped from=" & seat.activePhase.phaseName & " to=" &
-        wanted.phaseName & " tick=" & $packet.tick &
-        " reason=no_ready_modules")
-    else:
-      log("phase " & seat.activePhase.phaseName & " -> " & wanted.phaseName &
-        " at tick=" & $packet.tick)
-      seat.sendCall(ws, wanted, callJson, "phase_change")
+  let
+    previousCandidate = seat.candidatePhase
+    previousCount = seat.candidateViewCount
+    confirmed = seat.observePhase(wanted)
+  if wanted == seat.activePhase:
+    if previousCount > 0:
+      log("decision cleared phase=" & previousCandidate.phaseName &
+        " active=" & seat.activePhase.phaseName & " tick=" & $packet.tick)
+    return
+  if previousCount == 0 or previousCandidate != wanted:
+    log("decision candidate from=" & seat.activePhase.phaseName & " to=" &
+      wanted.phaseName & " views=1/" & $DecisionDebounceViews &
+      " tick=" & $packet.tick)
+  if not confirmed:
+    return
+
+  seat.candidateViewCount = 0
+  let callJson = seat.phaseCall(wanted)
+  if callJson.len == 0:
+    log("decision skipped from=" & seat.activePhase.phaseName & " to=" &
+      wanted.phaseName & " tick=" & $packet.tick &
+      " reason=no_ready_modules")
+  elif callJson == seat.activeCallJson:
+    log("decision applied from=" & seat.activePhase.phaseName & " to=" &
+      wanted.phaseName & " tick=" & $packet.tick &
+      " call=unchanged")
+    seat.activePhase = wanted
+    seat.phaseStartTick = packet.tick.int
+    seat.candidatePhase = wanted
+  else:
+    log("decision confirmed from=" & seat.activePhase.phaseName & " to=" &
+      wanted.phaseName & " views=" & $DecisionDebounceViews &
+      " tick=" & $packet.tick)
+    seat.sendCall(ws, wanted, callJson, "phase_change_debounced")
 
 proc handleShellMessage*(seat: ShellSeat; ws: WebSocket; message: Message) =
   case message.kind
diff --git a/bot/tests/shell_phase_test.nim b/bot/tests/shell_phase_test.nim
index a09c151..9246eaa 100644
--- a/bot/tests/shell_phase_test.nim
+++ b/bot/tests/shell_phase_test.nim
@@ -5,13 +5,13 @@ include ../baseline/shell_seat
 proc baseView(): StrategyView =
   StrategyView(
     tick: 900,
-    self: StrategySelf(pos: StrategyPoint(x: 100, y: 100), hp: 4,
+    self: StrategySelf(pos: StrategyPoint(x: 500, y: 500), hp: 4,
       hpFrac: 1.0, alive: true),
     partner: StrategyPartner(seat: 1,
-      pos: StrategyPoint(x: 250, y: 100), positionKnown: true,
+      pos: StrategyPoint(x: 620, y: 590), positionKnown: true,
       fresh: true, alive: true, aliveKnown: true, distance: 150.0),
     visibleEnemies: @[
-      StrategyEnemy(seat: 2, team: 1, pos: StrategyPoint(x: 350, y: 100),
+      StrategyEnemy(seat: 2, team: 1, pos: StrategyPoint(x: 750, y: 500),
         freshTick: 900, distance: 250.0, hp: 2, hpKnown: true,
         weakened: true)],
     zone: StrategyZone(present: true, phase: 1, ticksToShrink: 500,
@@ -22,38 +22,140 @@ proc strategist(): ShellSeat =
   result = newShellSeat(0)
   result.partnerSeat = 1
   result.selfTeam = 0
-  result.modules[result.moduleIndex(PlaybookCrossfireName)].state = msReady
-  result.modules[result.moduleIndex(PlaybookJackalName)].state = msReady
+  for module in result.modules.mitems:
+    module.state = msReady
+
+proc callFor(seat: ShellSeat; phase: StrategyPhase): JsonNode =
+  parseJson(seat.phaseCall(phase))
+
+proc controller(call: JsonNode; index: int): string =
+  call["plays"][index + 1]["play"].getStr
 
 suite "Season 2 phase strategy":
+  test "survival is guard-free and starts with asymmetric edge ride":
+    let
+      lower = strategist()
+      upper = strategist()
+    upper.slot = 16
+    for seat in [lower, upper]:
+      let call = seat.callFor(phaseSurvival)
+      check call.controller(0) == "edge_ride"
+      check call.controller(1) == "bodyguard"
+      check call.controller(2) == "supply_run"
+    check lower.callFor(phaseSurvival)["plays"][0]["params"]["never"] ==
+      %*["seat:0", "seat:1"]
+    check upper.callFor(phaseSurvival)["plays"][0]["params"]["never"] ==
+      %*["seat:1", "seat:16"]
+    for phase in StrategyPhase:
+      let call = lower.callFor(phase)
+      for entry in call["plays"]:
+        check not entry.hasKey("when")
+    check lower.callFor(phaseSurvival)["plays"][1]["params"]["margin"].getInt ==
+      BaseEdgeMargin
+    check upper.callFor(phaseSurvival)["plays"][1]["params"]["margin"].getInt ==
+      BaseEdgeMargin + PartnerMarginOffset
+
+  test "the guard-free decision table changes controller order":
+    block lowHpFraction:
+      let seat = strategist()
+      var view = baseView()
+      view.self.hpFrac = 0.66
+      let phase = seat.desiredPhase(view)
+      check phase == phaseLowHp
+      check seat.callFor(phase).controller(0) == "supply_run"
+      check seat.callFor(phase).controller(1) == "edge_ride"
+
+    block lowHpRaw:
+      let seat = strategist()
+      var view = baseView()
+      view.self.hp = 1
+      check seat.desiredPhase(view) == phaseLowHp
+
+    block partnerFar:
+      let seat = strategist()
+      var view = baseView()
+      view.partner.distance = PartnerFarDistance + 1.0
+      let phase = seat.desiredPhase(view)
+      check phase == phasePartnerFar
+      check seat.callFor(phase).controller(0) == "bodyguard"
+      check seat.callFor(phase).controller(1) == "edge_ride"
+
+    block zoneUrgent:
+      let seat = strategist()
+      var view = baseView()
+      view.zone.ticksToShrink = ZoneUrgencyTicks
+      let phase = seat.desiredPhase(view)
+      check phase == phaseZoneUrgent
+      check seat.callFor(phase).controller(0) == "edge_ride"
+
+    block nearZoneEdge:
+      let seat = strategist()
+      var view = baseView()
+      view.self.pos.x = BaseEdgeMargin - 1
+      check seat.desiredPhase(view) == phaseZoneUrgent
+
+    block outsideZone:
+      let seat = strategist()
+      var view = baseView()
+      view.self.pos.x = -1
+      check seat.desiredPhase(view) == phaseZoneUrgent
+
+    block zoneOverridesPartnerFar:
+      let seat = strategist()
+      var view = baseView()
+      view.partner.distance = PartnerFarDistance + 1.0
+      view.zone.ticksToShrink = ZoneUrgencyTicks
+      check seat.desiredPhase(view) == phaseZoneUrgent
+
+    block partnerStacked:
+      let seat = strategist()
+      var view = baseView()
+      view.partner.pos = StrategyPoint(x: 520, y: 500)
+      view.partner.distance = PartnerStackDistance - 1.0
+      check seat.desiredPhase(view) == phaseSurvival
+
+    block partnerStale:
+      let seat = strategist()
+      var view = baseView()
+      view.partner.fresh = false
+      view.partner.distance = PartnerFarDistance + 1.0
+      check seat.desiredPhase(view) == phaseSurvival
+
   test "a favourable visible fight selects crossfire":
     let seat = strategist()
     check seat.desiredPhase(baseView()) == phaseCrossfire
 
+  test "a partner in the target corridor blocks a fight phase":
+    let seat = strategist()
+    var view = baseView()
+    view.partner.pos = StrategyPoint(x: 600, y: 500)
+    view.partner.distance = 100.0
+    check seat.desiredPhase(view) == phaseSurvival
+
   test "a recent kill and two weak enemies select jackal":
     let seat = strategist()
     var view = baseView()
     view.visibleEnemies.add(StrategyEnemy(seat: 3, team: 2,
-      pos: StrategyPoint(x: 450, y: 100), freshTick: 900,
+      pos: StrategyPoint(x: 850, y: 600), freshTick: 900,
       distance: 350.0, hp: 1, hpKnown: true, weakened: true))
     view.killFeed.add(StrategyKill(tick: 890, killerTeam: 2,
       victimSeat: 8))
     check seat.desiredPhase(view) == phaseJackal
 
-  test "fight phase recalls on every documented safety condition":
+  test "fight phase recalls to the matching safe order":
     block zoneUrgency:
       let seat = strategist()
       seat.activePhase = phaseCrossfire
       var view = baseView()
       view.zone.ticksToShrink = ZoneUrgencyTicks
-      check seat.desiredPhase(view) == phaseSurvival
+      check seat.desiredPhase(view) == phaseZoneUrgent
 
     block lowHp:
       let seat = strategist()
       seat.activePhase = phaseCrossfire
       var view = baseView()
       view.self.hpFrac = 0.66
-      check seat.desiredPhase(view) == phaseSurvival
+      check seat.desiredPhase(view) == phaseLowHp
 
     block partnerDeath:
       let seat = strategist()
@@ -77,3 +179,12 @@ suite "Season 2 phase strategy":
       var view = baseView()
       view.visibleEnemies.setLen(0)
       check seat.desiredPhase(view) == phaseSurvival
+
+  test "a phase change needs two consecutive views":
+    let seat = strategist()
+    check not seat.observePhase(phaseLowHp)
+    check seat.candidateViewCount == 1
+    check seat.observePhase(phaseLowHp)
+    seat.activePhase = phaseLowHp
+    check not seat.observePhase(phaseLowHp)
+    check seat.candidateViewCount == 0

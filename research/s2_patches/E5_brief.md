You are fixing the season-2 duo formation of our Paintbot policy in /Users/jordan/Desktop/Projects/johomax/johomax_ctf (git, branch main). The working tree already carries the E4 guard-free ladder as UNCOMMITTED changes in bot/baseline/shell_seat.nim and bot/tests/shell_phase_test.nim (design: analysis/s2_orchestrator.md "Guard-free ladder"). Read first: AGENTS.md; analysis/s2_orchestrator.md (all); analysis/s2_shell.md §2-§5; sim/README.md "Season 2 locally"; the last three sections of research/LEDGER.md. Engine reference plays (read-only): /private/tmp/engine-main-v40/play_sdk/reference/{bodyguard,edge_ride,crossfire,target_law}.nim; engine body (read-only): /private/tmp/engine-main-v40/src/shell/{body,ladder,episode}.nim. Nim: export PATH=$HOME/.nimby/nim/bin:$PATH. shell_view_test needs the engine on the path: --path:/private/tmp/engine-main-v40/src (check how the file imports ctf/sim_types).

MEASURED FACTS (local real server, 32-seat self-mirror, seeds 1400-1403, E4 base ladder with never:["seat:a","seat:b"] accepted):
- Same-colour kills 9 vs enemy kills 7 (gate FAILED; HEAD was 12 vs 8).
- Mechanism seen in episodes/e4-e4-base-mirror-s1400-n4/seed_00001402/server.log: nobody moves during ticks 0-700 (FIRST_LIGHT_MOVEMENT moving=0); from ~700 on, ~25 of 32 seats are "aiming" while only 2-7 move. The first gunfire of the game (tick 1461) is the two seats of duo pale-blue (seats 15 and 31) hitting EACH OTHER on the same tick, again at 1480 and 1499 (the gun cooldown cadence), both dying at 1499. Red (seats 0/16?) identical at 1479/1498/1517. i.e. the duo is still STACKED on its spawn pixel when the hold releases and each partner's shot lands on the other (the corridor blind spot). The E4 margin asymmetry (M vs M+80) never separates them because edge_ride emits hold whenever the seat is already inside the margin band (edge_ride.nim:95-120) — the duo never leaves the spawn until the zone forces it.
- The engine's bodyguard controller PUSHES AWAY from its ward when closer than leashMin (bodyguard.nim:116-117: `if d < sq(leashMin): point = ward.projectFrom(self.pos, leashMin)`) and holds when inside [leashMin, leashMax]; crossfire does the same with spacingMin (crossfire.nim:168). The ward is the configured duo partner (bodyguard.nim:4-5). The first controller entry is the only one that runs (guards are zero-valued), so a seat whose call starts with bodyguard moves relative to the partner and NOT relative to the zone; our 4 Hz strategist must re-call edge_ride-first when the zone is urgent (E4 already does).

IMPLEMENT (bot/baseline/shell_seat.nim; keep the E4 structure, logging and hardening):
1. Asymmetric formation in the calm survival state: the LOWER seat of the duo (slot < 16) is the leader: order edge_ride, bodyguard, supply_run (as E4). The UPPER seat (slot >= 16) is the follower: order bodyguard{leash [SepMin, SepMax], interpose false, peelHp 2}, edge_ride, supply_run. Constants SepMin/SepMax sweepable; base SepMin=128, SepMax=260.
2. Keep E4's zone-urgent rule (edge_ride first for both seats) and low-HP rule, but the follower returns to bodyguard-first as soon as the zone is calm again. When the partner is dead or its track has never been seen, the follower uses the leader order.
3. Add a periodic geometry log line from the 4 Hz strategist (every ~5 s of game time and on every phase change): "[s2] geo tick=<t> hp=<hp> partner_d=<px or -1> partner_fresh=<0/1> zone_edge=<px> alive_teams=<n> phase=<name>" so batches can verify the separation. Partner distance comes from the decoded PV1 view (shell_view.nim); if the view has no partner track, log -1 and say so in the report.
4. Fight entry (crossfire/jackal) stays as in E4, except: the "partner closer than 40 px suppresses fight" rule must NOT suppress separation — the follower's bodyguard order already separates.
Tests: extend bot/tests/shell_phase_test.nim (follower calm → bodyguard first with the leash; leader calm → edge first; both zone-urgent → edge first; follower with dead partner → leader order; geometry log format). Run shell_phase_test and shell_view_test.
BUILD native binaries (cd bot && nim c -d:release -d:useMalloc --opt:speed --hints:off --warning:UnusedImport:off --nimcache:/tmp/e5-cache-<name> --out:/tmp/e5-bot-<name> baseline.nim): `e5-sep128` (SepMin 128, SepMax 260), `e5-sep200` (SepMin 200, SepMax 320), and `e5-sym128` (BOTH seats bodyguard-first with leash [128,260] in calm survival — symmetric variant), plus the E4 base as /tmp/e5-bot-e4base (current tree before your edits: build it FIRST). Save each variant's diff versus HEAD as /tmp/e5-<name>.patch (the final tree should hold the e5-sep128 variant, uncommitted; do not commit; do not touch .git otherwise). Write /tmp/e5-runbook.md with the batch lines (scripts/s2_local.py batch ... --server /tmp/johomax-ctf-server-runtime2 --engine /private/tmp/engine-main-v40 --config /tmp/johomax-s2-config.json --seconds 900, ports 2041+): self-mirror n=4 seeds 1400-1403 for each variant (pass gate: same-colour "killed by" lines near zero, enemy kills >= 7), then 8/8 vs /tmp/e5-bot-e4base n=8 with colour rotation, then 1/5/5/5 vs starter:aggressive/cautious/collaborative n=8. You cannot open sockets; the human runs the batches. Report: what you changed, the exact call JSON for leader and follower, test results, and any doubt about whether bodyguard's ward is the partner on the deployed engine.

ADDITIONAL FACTS (found after the first draft of this brief; they change the priorities):
- The E4 low-HP rule misfires: 1190 low_hp re-calls across 128 seats in the four gate games, nearly all at tick ~742 and BEFORE any gunfire. Cause: the zone config (/tmp/johomax-s2-config.json zonePhases) has phase 0 z=0.75 waitTicks 345 + shrinkTicks 213 (dps 0), then phase 1 z=0.55 shrinkTicks 244 dps 3, so zone damage starts around tick 560-800 for every seat still outside, and one point of damage on a 3-hp cog gives hp_frac 0.667 < 0.67. From tick ~749 every seat therefore ran supply_run FIRST (the same shadow as HEAD), so the E4 gate never measured the formation. FIX: low HP means `hp <= 1` only (drop the hp_frac test, or use hp_frac <= 0.34), and never while the zone is urgent.
- Nobody moves during ticks 0-700 although edge_ride (edge_ride.nim:85-98) should navigate to the NEXT rectangle's margin once ticksToShrink <= enterLead (120). Either the deployed view lacks `next`/`ticksToShrink` (check shell_view.nim's decode of the zone section and the engine's src/shell/binary_view.nim + episode.nim view builder to see whether next-rect and ticks-to-shrink are populated) or the seats are already inside the band and hold. Include `zone_next=<0/1> ticks_to_shrink=<n or -1>` in the geo log line so the batches settle it, and state in the report what the engine populates.
- Partners really are on the same pixel at spawn (the mutual hits on identical ticks); the formation change is the fix, the geo log verifies it (partner_d should read >= SepMin within ~100 ticks of the first accepted call).
